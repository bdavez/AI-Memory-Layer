In public release mode, worker agents and the agent server run in a sandboxed, mock configuration. They demonstrate architecture and heartbeat flows without connecting to real hardware, networks, or language models.


This folder contains archived versions of the original worker agents.
They are preserved for portfolio and architectural reference only.
These files are not used in the public release build and should not be executed.